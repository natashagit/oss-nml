[project]
name = "ai-adapter"
version = "0.1.0"
description = "Adapter that calls the AI FastAPI service"
requires-python = ">=3.11"
dependencies = [
    "ai-api",
    "ai-service-client",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
