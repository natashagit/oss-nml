# AI Client Adapter Reference

`ai_client_adapter` provides the `ServiceClient` adapter that wraps the generated service client and implements the `ai_client_api.Client` interface. The API surface below is generated from package docstrings.

::: ai_client_adapter

